# React + TypeScript + Vite

# Fullstack Chat (React + Vite + Express + OpenAI)

Минималистичное fullstack-приложение:

- **Frontend**: React + TypeScript + Vite
- **Backend**: Node.js + Express
- **ИИ**: OpenAI Chat Completions API
- **Опционально**: голосовой ввод через Web Speech API

## Требования

- Node.js 18+
- pnpm

## Настройка переменных окружения

1) Скопируйте пример:

```bash
copy .env.example .env
```

2) Откройте `.env` и укажите ключ:

```
OPENAI_API_KEY=...
```

## Установка

В корне проекта:

```bash
pnpm i
```

В папке `server/`:

```bash
pnpm --dir server i
```

## Запуск

Запуск фронта и бэка вместе:

```bash
pnpm dev:full
```

- **Frontend**: http://localhost:5173
- **Backend**: http://localhost:8787

Во время разработки фронт обращается к API через Vite proxy (`/api -> http://localhost:8787`).

## API

- `GET /api/health` — проверка сервера
- `POST /api/chat` — отправка сообщения в ИИ

Пример тела запроса:

```json
{ "message": "Привет!" }
```

Пример ответа:

```json
{ "requestId": "...", "answer": "..." }
```

## Голосовой ввод

Кнопка 🎤 использует Web Speech API (в некоторых браузерах доступно как `webkitSpeechRecognition`).
Если браузер не поддерживает API — кнопка будет выключена.
